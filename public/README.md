# Public Assets Folder

Поместите сюда ваши медиафайлы (например, MP3 трек `track.mp3` или аудиофайлы).

Все файлы из этой папки будут доступны напрямую в проекте по их имени:
например, файл `/public/track.mp3` будет доступен по ссылке `/track.mp3`.
